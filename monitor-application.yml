eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka/
    registryFetchIntervalSeconds: 1
  instance:
    leaseRenewalIntervalInSeconds: 1

turbine:
  aggregator:
    clusterConfig: web-apps
  appConfig: admin-application, news-application
